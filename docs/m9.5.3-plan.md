# M9.5.3 — Flujo de título: plan de trabajo

Estado del reconocimiento (2026-09-02, repo @ 045b2d1):

## Lo que YA existe en Petari (decompilado, sin compilar en nuestro build)

| Pieza | Ruta | Estado |
|---|---|---|
| `TitleSequenceProduct` | `src/Game/Screen/TitleSequenceProduct.cpp` | **COMPLETO**: la pantalla de título real (logo `TitleLogo` + `PressStart`, nerves BgmPrepare→LogoFadein→LogoWait→LogoDisplay→Decide→Dead, input A+B, anims Appear/Wait/Decide/ReactionA/ReactionB/End/ButtonReaction). |
| `LayoutActor` | `src/Game/Screen/LayoutActor.cpp` | Completo (130 líneas). Depende de LayoutManager (falta), PaneEffectKeeper ✓, Spine ✓, LayoutCoreUtil ✓. |
| `SimpleLayout` | `src/Game/Screen/SimpleLayout.cpp` | Completo (ctor → `initLayoutManager(nombre, a3)`). |
| `LayoutCoreUtil` | `src/Game/Screen/LayoutCoreUtil.cpp` | Existe (89 líneas). |
| `ResourceInfo` (ResTable) | `src/Game/System/ResourceInfo.cpp` | Existe. |
| `LayoutHolder` | `src/Game/System/LayoutHolder.cpp` | **Parcial**: ctor sí; `initializeArc()` comentado; `GetResource`/`GetFont`/`mount`/`initEachResTable` solo declarados → reconstruir. |
| `LayoutAnmPlayer` | `src/Game/Animation/LayoutAnmPlayer.cpp` | Existe (reproductor de brlan por layout). |
| `LogoFader` | `src/Game/Screen/LogoFader.cpp` | Ya compilado (no usa layouts, solo fill-screen). |
| `FileSelector` | `src/Game/Map/FileSelector.cpp` | Completo pero ENORME (1585 líneas, ~40 nerves, Mii/RFL/guardado/cámara 3D) → **fuera de M9.5.3** (M10). |
| `GameSequenceProgress` | `src/Game/System/GameSequenceProgress.cpp` | Casi completo; **falta el cuerpo de `requestGalaxyMove`** (comentario `// GameSequenceProgress::requestGalaxyMove`) → reconstruir. |
| `SceneFactory` | parche compat | Solo Game/Intermission/Logo → registrar "Title". |

## Lo que NO existe en Petari (reconstrucción compat)

- **`TitleScene`** (clase Scene que aloja TitleSequenceProduct; en el juego real
  también FileSelector + fondo 3D FileSelectSky — para M9.5.3 solo el
  TitleSequenceProduct sobre fondo negro, documentado).
- **`LayoutManager`** (solo header `include/Game/Screen/LayoutManager.hpp` con
  layout de miembros completo) — el corazón: monta `/LayoutData/<Nombre>.arc`
  (vía `MR::mountArchive`, real desde M9.5.1), crea el `LayoutHolder`, hace
  `nw4r::lyt::Layout::Build` (con `convertBrlyt` de M9.5.2), gestiona
  DrawInfo/paneCtrls/animTransforms y movement/calcAnim/draw.
- **brlan real**: `AnimResource::Set` + `AnimTransformBasic::Bind/Animate`
  (stubs WARN en `compat/nw4r/LytMissing.cpp`) + swapper BE de RLAN.
- Utilidades MR de layout/anim usadas por TitleSequenceProduct: `startAnim`,
  `isAnimStopped`, `setAnimFrameAndStop`, `emitEffect` (stub OK),
  `testCorePadButtonA/B` (input compat M6), `startStageBGM`/`isPreparedStageBgm`
  (audio stub → debe devolver "listo" para no bloquear el nerve).

## Convenciones de assets confirmadas en el código

- Un archivo por layout: `/LayoutData/TitleLogo.arc`, `/LayoutData/PressStart.arc`,
  `/LayoutData/WiiRemoteStrap.arc`, etc.
- Fuentes: `/LayoutData/Font.arc` (GameSystemFontHolder) y `EmbeddedFont.arc`.
- Pendiente de confirmar con el listado del usuario: nombres exactos presentes
  en su dump.

## Sub-pasos

- **M9.5.3a — Pila de layouts real (carga)**: ✅ **HECHO** (satisfactorio en
  sandbox: 181 tests verdes + boot smoke sin assets degrada con ERROR
  elegante). Lo que se hizo realmente:
  - Vendored limpio compilado: `LayoutActor.cpp`, `SimpleLayout.cpp`,
    `LayoutPaneCtrl.cpp` (descubierta en la recon: las capas de animación
    viven en el LayoutPaneCtrl RAÍZ, no en el LayoutManager).
  - Copias parcheadas: `LayoutHolder.cpp` (initializeArc reconstruido: escaneo
    RARC vía countResource/getDirEntry, clasificación .brlyt/.brlan/otros en
    ResTables, GetResource 'timg'/'font' con fallback sin extensión; GetFont
    devuelve null hasta 3c), `LayoutAnmPlayer.cpp` (guarda null en start() +
    isStop() reconstruido: null transform o state-bit 1 del J3DFrameCtrl),
    `ResourceInfo.cpp` (include MSL_C → cstddef), `PaneEffectKeeper.cpp`
    (stub completo: el sistema de efectos es M10+).
  - `LayoutManagerCompat.cpp`: LayoutManager reconstruido (mount de
    `/LayoutData/<nombre>.arc`, convertBrlyt + Layout::Build, AnimTransforms
    por brlan via CreateAnimTransform — null hasta 3b, root pane ctrl con las
    capas de anim, movement/calcAnim reales, draw stub hasta 3c, todo
    tolerante a mLayout null) + helpers MR:: verbatim de LayoutUtil/
    LayoutCoreUtil (isDead/isExecute*/startAnim familia/isAnimStopped/
    convert*Pos/copyPaneTrans/getScreenWidth=608/getScreenHeight=456).
  - `J3DFrameCtrlCompat.cpp`: init/checkPass/update VERBATIM del vendored
  J3DAnimation.cpp (el resto de J3D llega en M9.5.4).
  - Shadow `include/Game/Screen/LayoutManager.hpp`: superficie de métodos
    idéntica; miembros 0x64-0x78 sin nombre → campos host con nombre.
  - Stubs viejos eliminados: LayoutActor en GameBoot.cpp, SimpleLayout +
    familia startAnim en SceneCompat.cpp; createSimpleLayout ahora es el real
    (drawType 60 + initWithoutIter).
  - Desviaciones del plan: LayoutCoreUtil NO se compila (depende del sistema
    de mensajes; sus 2 funciones útiles se copiaron verbatim). El ctor de
    StarPointerLayoutTargetKeeper se copió verbatim en LayoutManagerCompat.
  - Tests nuevos (`layout_holder_test.cpp`, 3): clasificación de RARC
    sintético + GetResource, isStop con transform null, J3DFrameCtrl
    stop/loop. OJO: los nombres en la tabla de strings de un RARC real van en
    MINÚSCULAS (la búsqueda lowercasea la consulta) — el test lo refleja.
  - `scene_test.cpp`: logo_fader_nerve_timeline añade `fader.appear()` — el
    movement() real early-outea con el actor muerto (kill() en el ctor del
    fader); LogoScene real ya llama appear().
  - Hito visible en el boot del usuario: boot.log con
    `LayoutManager 'WiiRemoteStrap': built from '/LayoutData/WiiRemoteStrap.arc'`
    + líneas INFO compat.tpl (texturas convertidas durante Build) +
    `GetFont: global font holder not wired yet`. Si el arc no existe: ERROR
    `cannot mount` y el boot sigue.
- **M9.5.3b — brlan real**: swapper BE RLAN + AnimResource::Set +
  AnimTransformBasic::Bind/Animate (sustituyen los stubs) + startAnim/isAnimStopped
  reales vía LayoutAnmPlayer. Tests: brlan sintético (bind → animate → valores
  de pane interpólados).
- **M9.5.3c — Dibujo de layouts**: LayoutManager::draw → Layout::Draw(DrawInfo)
  con la capa GX compat (GXBegin/quads/proyección/scissor/blend ya existen);
  texto con CharWriter (fuentes brfnt ya convertidas). Hito visible: el strap/
  logo se VE en la ventana.
- **M9.5.3d — TitleScene**: requestGalaxyMove reconstruido (tipo 7 → "Title";
  tipos de galaxia quedan para M9.5.4), TitleScene compat (TitleSequenceProduct
  real sobre fondo negro), SceneFactory registra "Title", input A+B mapeado.
  Hito visible: Logo → título con animaciones → A+B → Decide (la transición a
  GameScene llega en M9.5.4).

## Riesgos conocidos

- `FileSelector`/guardado/Mii: fuera de alcance; TitleScene de M9.5.3 termina
  en "Decide" y lo documenta en el log.
- Audio stub: `isPreparedStageBgm` debe devolver true (si no, el nerve
  BgmPrepare se cuelga para siempre).
- Efectos (`emitEffect`, PaneEffectKeeper): stub silencioso aceptable.
- MSVC: `offsetof` sobre polimórficos (shadows M9.5.2) pendiente de confirmar
  en la máquina del usuario.
